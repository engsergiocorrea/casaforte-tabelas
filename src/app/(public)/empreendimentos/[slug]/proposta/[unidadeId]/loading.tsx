import { SplashScreen } from '@/components/SplashScreen'

// Tela de carregamento exibida durante a transição para esta rota (Next mostra
// enquanto a página carrega e remove sozinho quando fica pronta).
export default function Loading() {
  return <SplashScreen />
}
